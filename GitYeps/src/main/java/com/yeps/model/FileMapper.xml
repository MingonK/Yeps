<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yeps.model">
	<insert id="insertFile" parameterType="FileDTO">
		insert into yeps_file values (yeps_file_seq, #{mnum, jdbcType=VARCHAR}, #{rnum, jdbcType=VARCHAR}, #{rvnum, jdbcType=VARCHAR}, #{evnum, jdbcType=VARCHAR}, 
				#{filename}, #{origin_filename}, #{filesize})
	</insert>
</mapper>